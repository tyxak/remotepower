A group needs at least one letter, digit, _, - or /
A label for this IP (e.g. "mx1"), or leave blank:
A reason is required to start a litigation hold
A tag needs at least one letter, digit, _, - or /
AI did not return a clean rule — see the response
AI provider not configured. Configure in Settings → AI.
Active scans send attack traffic — tick the authorization box first.
Add at least one escalation tier (minutes)
Alert not found — it may have been purged
Allow pop-ups for this site to open the printable report
Allow pop-ups to open the printable report
An archive is already running — cancel it first
Apply the previewed configuration changes? This overwrites the affected config collections.
Ask a question about your fleet
Audit log cleared (pre-wipe archive saved)
Auto-patch will hold this package back on Linux hosts, pinning it immediately
Billable hours need a customer (site) or a device
Browser notifications are blocked for this site
CIDR must look like 10.0.0.0/24 or 2001:db8::/48
Change submitted — awaiting approval by another admin (Confirmations page)
Change your password first — every other action is blocked.
Clear EVERY device’s pending queue? Nothing currently waiting will be delivered. Commands already picked up by an agent are unaffected.
Clear all resolved (approved / rejected / expired) entries? Pending entries are kept.
Clear the dispatched-command log? This clears the command history shown here and on the Command History page.
Clear the recent jobs list? This only clears the tracker — anything already queued on a device still runs.
Clear the reported browser-error ring? After a fix ships, the list refilling (or staying empty) is the verification.
Clearing the audit log is gated. A pre-wipe archive is saved automatically. Enter your admin password to confirm:
Command queued — agent will collect and send config in ~60s
Compose YAML must contain a "services:" block
Copy failed — your browser may block clipboard access on http://
Copy this dashboard layout code:
Could not enable notifications:
Cron filled — adjust if needed, then save.
Cron needs 5 fields (min hour day month weekday)
Current config loaded — review and click Save to apply as desired
DKIM selector to check (e.g. "google", "default"), or leave blank:
Demo mode — read-only sandbox. Nothing was changed.
Describe the rule in plain English:
Describe what the script should check
Describe what the script should do
Describe what the script should do:
Domain to monitor (e.g. example.com):
Dry-run sent — waiting for the agent…
Enter a
Enter a cron expression (e.g. 0 3 * * *).
Enter a cron schedule (or disable the schedule)
Enter a cron schedule (or untick Email on schedule)
Enter a device, group, or tag
Enter a process and/or proto/port
Enter a subnet, e.g. 192.168.1.0/24.
Enter a tag or group value
Enter hours between 0.25 and 8760, or leave blank for permanent
Enter your admin password to confirm — this signature is what every agent trusts.
Every device carrying this tag is updated in one operation.
Feedback saved — it sharpens future triage
File too large to download through the agent channel (first 256 KiB only)
File was truncated on read — refusing to overwrite
Filter rule added (disabled) — review then enable
Fix parked — a second admin must approve it
Form filled from AI draft — review, then Add (it stays disabled)
Generate a status token? Any previous token stops working.
HTTP target must be a full URL (https://host/path)
Host summary copied — paste it anywhere
Job added — have it curl the ping URL when it finishes
Key rotated — the old key is now inactive
LAN discovery schedule created.
Link copied — opens straight to this device view
Listen port must be a whole number 1–65535
Listing archives at the destination…
Live view armed — samples appear on the next heartbeat
Load the patch report first (click Refresh)
Log sweep queued — collecting on the next heartbeat
Monitor not found in the saved config — refresh and retry
Monitoring — checking…
NAT rule added (disabled) — review then enable
Name this passkey (e.g. "YubiKey", "Phone"):
Name, device, and compose file are all required
New VMID for the clone (must be unused):
New certificate issued — download the files now
New name for this client.
No archived audit entries yet — the live log is still within its retention window.
No compose stacks yet. Click "New stack" to upload a docker-compose file and deploy it to a device.
No compose-managed host found for this image — update it manually (compose pull && up -d).
No groups in use. Set a device’s group from its drawer.
No tags in use. Tag a device from its drawer, or in bulk from the Devices page.
Opened the posture report in a new tab
Opened the printable report in a new tab
Opening the print dialog — choose "Save as PDF"
Parked for a second admin to approve (change-approval is on).
Parked — a second admin must approve it
Passkey registered.
Passkey registration cancelled.
Passkey removed.
Passkey sign-in cancelled.
Passphrase must be at least 12 characters
Passphrase must be at least 8 characters
Passphrase rotated.
Passphrases do not match
Passphrases do not match.
Password required.
Paste a config to import
Paste a dashboard layout code to apply:
Paste an SSH public key
Path must be absolute (start with /)
Perf HUD
Poll failed — see error below
Poll interval must be 30–3600 seconds
Polled OK — see Latest poll below
Polling over SSH…
Popup blocked — allow popups to view the thread
Port must be between 1 and 65535
Prefilled — saving adds a new check
Preview first
Preview the command first
Preview the waves first
Price per unit, excluding VAT
Print target missing
Probing — this can take a while for many targets
Profile assigned
Proposal failed — is the AI executor module enabled?
Provide an SSH key or password
Provisioning command queued.
Pull this backup back and OVERWRITE the restore path. Enter the absolute directory on the host to restore into:
Quantity (e.g. hours or units)
Quantity and price must be numbers
Query duplicated
Queued
Queued — follow it on Rollouts → Recent jobs
Recovery bundle exported — store it off this machine
Refreshing KEV/EPSS feeds…
Reindex:
Reject this MCP write action. Optional note:
Rejected
Remove every finished scan (done / failed / cancelled) and all of their findings? This cannot be undone.
Remove this destination?
Remove this email route?
Removed from the catalog
Removes the KMIP activity history. The clear itself is recorded
Rename query:
Reset all monitor alert state? This allows alerts to re-fire.
Reset the dashboard to its default layout?
Reset to default — Save to apply
Restart dispatched — reconnecting…
Restart scheduled
Restore path must be absolute
Restore queued — output appears in the device command history
Restored — this line alerts again
Resumed update alerts
Reveal failed:
Reveal still refused after checkout — check the audit log.
Revoke ALL sessions? Everyone (including you) will need to log in again.
Revoke failed:
Revoke this session? That browser/device will be signed out.
Rotation failed:
Rule added (disabled) — review then enable
Rule already exists
Rule queued — applies on the next agent check-in
Run a query first
SLO object not found — refresh and retry
Satellite name (e.g. dmz-relay):
Saved as the default for new tunnels.
Saving & pushing spans…
Scan failed
Schedule created.
Schedule fired — scan(s) queued.
Scope value required for
Scope value, label and password are required.
Script body is required
Script flagged as dangerous — tick the box to confirm
Script inserted — review before saving
Script name is required
Search and pick a device above first.
Search and pick a device first (or use a verified target’s Schedule button).
Search error:
Search for and pick a device
Select a
Send a test email via your SMTP to confirm outbound delivery works:
Send failed
Sending test entry…
Sending…
Set a default SSH username first (Settings → Security → SSH preferences)
Set up the CMDB vault first (Fleet → CMDB), then unlock it here.
Sign out all other sessions? Only this browser will stay signed in.
Signing key generated — now distribute the public key to agents
Silence this exact (host, event) for how many hours?
Silence window must be 1–1440 minutes
Site id to quote for (see the Sites page).
Skipped:
Snapshot deletion queued.
Snapshot deletion queued:
Specify at least one event or severity
Staged rollout needs at least one ring (e.g. "canary = tag:canary")
Start time is required
Starting DSM upgrade over SSH…
Status token generated
Stored in vault (encrypted)
Subject and cron are required
Subject required
Swap the live configuration for this snapshot? The current state is saved as a revision first, so you can restore back.
Tags to ADD (comma-separated, leave blank for none):
Tags to REMOVE (comma-separated, leave blank for none):
Target % must be between 0 and 100 (exclusive)
Target added — publish the proof shown, then Verify.
Target node to migrate to:
Target not found
Task not found
Template not found
Test failed (no response)
Test webhook sent successfully!
Testing IMAP…
That device is not enrolled
That module is switched off (Settings → Advanced).
The AI module is not loaded
The agent ran but found no DNS credentials in acme.sh on that host.
The web terminal opens an SSH session, which needs OpenSSH
There is no other group to merge into
There is no other tag to merge into
This alert carries neither a matched line nor its rule pattern —
This credential must be checked out before it can be revealed.
This is the only copy — a past-dated posture report cannot be
This mute has already lapsed — reload the list
This overwrites files under the restore path. Type RESTORE to proceed.
This server already holds KMIP keys. Restoring replaces all of them, and any key not in the bundle is lost. Continue?
Threshold must be a positive whole number
Ticket assigned to group
Ticket assigned to you
Timed out waiting for the agent — is it online and on the latest build?
Title is required.
Tour complete — explore away!
URL required
Un-cleared — this line alerts again
Unfilled placeholders left as-is:
Unlock failed:
Unlock the vault first (top of this page).
Update script completed
Upgrade started — router is rebooting
Username may use letters, digits, dot, dash, underscore (max 32)
VMID must be a number
VNC server password:
Vault is locked — please unlock and retry.
Vault locked
Vault setup failed:
Vault unlocked
Watcher and target are required
Webhook URL cleared — click Save to apply
What are you quoting for? (e.g. "Firewall replacement — labour")
What fixed it? (optional — it is kept on the alert and shows in
Widgets aligned
WireGuard helper script not loaded
Write something first
Write the email body first
You have unsaved Settings changes. Leave without saving?
